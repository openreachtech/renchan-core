describe('Seeded priority group 02', () => {
  describe('Second', () => {
    const cases = [
      1041,
      1042,
      1043,
    ]

    test.each(cases)('value: %s', async value => {
      expect(value)
        .toBe(value)
    })
  })
})
